import './spinner.scss';
import React, { FC } from 'react';

const Spinner: FC = () => (
  <div className="spinner">
    <div className="spinner__inner">
      <div />
    </div>
  </div>
);

export default Spinner;
